type Rect = struct {
    width: int,
    height: int
};

func area(r: Rect) -> int {
    r.width * r.height;
}

my_rect : Rect = Rect { width: 10, height: 5 };

# Modify a property
my_rect.width = 20;

area(my_rect); # Should return 100
